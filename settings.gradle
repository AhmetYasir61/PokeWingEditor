pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // LWJGL Android portu ve benzeri Minecraft/JVM bagimliliklari icin
        maven { url 'https://jitpack.io' }
    }
}

rootProject.name = "PokeWingEditor"
include ':app'
